package io.github.tavstaldev.openChat.models;

/**
 * The EOperator enum represents various comparison operators
 * that can be used to evaluate conditions.
 */
public enum EOperator {
    /** Represents the equality operator (==). */
    EQUALS,

    /** Represents the not-equal operator (!=). */
    NOT_EQUALS,

    /** Represents the greater-than operator (>). */
    GREATER_THAN,

    /** Represents the less-than operator (<). */
    LESS_THAN,

    /** Represents the greater-than-or-equal operator (>=). */
    GREATER_THAN_OR_EQUAL,

    /** Represents the less-than-or-equal operator (<=). */
    LESS_THAN_OR_EQUAL
}
